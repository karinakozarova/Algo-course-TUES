#include <iostream>
#include <cmath>


using namespace std;

long long int formula(int x){
	long long int result = pow(x,1.3) + 3*pow(x,1.7)+ 9* pow(x,0.3);
	return result;
}



int main(){
	unsigned long long int c;
	cin >> c; // stoinost



	return 0;
}