#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int	sort_steps(int *arr,int count){
	return 0;
}


int main() {
 	int count;
	cin >> count;
	int arr[count];
	for(int i = 0; i < count;i++){
		cin >> arr[count];
	}


	cout << sort_steps(arr,count);
    return 0;
}

